'use client'
import React from 'react';

const AnimalDetails = ({params}) => {
    const {name}=params;
    console.log(name);
    return (
        <div>
            <h1>hjf</h1>
        </div>
    );
};

export default AnimalDetails;